import { Button } from "components/Button/Button";
import React from "react";

export default function AccountPage() {
  return (
    <div>
      <Button variant="primary">Siema</Button>
      <Button variant="secondary">Siema2</Button>
    </div>
  );
}

query GetCategories {
  categories {
    ...Categories
  }
}

query GetProductsByCategorySlug($categorySlug: String!) {
  category(where: { slug: $categorySlug }) {
    id
    name
    products {
      ...ProductDisplay
    }
  }
}
